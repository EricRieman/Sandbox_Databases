//if there is a function inside of the module that gets invoked, that function
//will run, even if we dont assign it to a variable or export anything from the 
//module. The key being whenever you require a module, you invoke it

require('./MindGenade')
